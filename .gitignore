# -----------------------------------------------------------------------------
# CONTRACT: .gitignore
# Purpose: keep Consol's repository inspectable.
# Non-goals: commit build output, caches, or machine-specific files.
# -----------------------------------------------------------------------------

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.#*
*.tmp

# --- IDEs ---
.idea/
.vscode/
*.iml

# --- Clojure / Leiningen build output & caches ---
/target/
/classes/
/out/
/.checksum
/.cpcache/
/.nrepl-port
/.nrepl-port
/.repl-port
/repl-port
/.lein-*
/.lein-*
/.lein/
/pom.xml
/pom.xml.asc
/*.jar
/*.class

# clj-kondo / lsp / misc tooling
/.clj-kondo/.cache/
/.lsp/
/.calva/

# --- Node / JS ---
/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.yarn/
.cache/

# Lockfiles:
# Keep package-lock.json for reproducible builds; ignore other managers unless you use them.
yarn.lock
pnpm-lock.yaml

# --- shadow-cljs output (build artifacts) ---
/resources/public/js-out/
/resources/public/js-out/**

# Source maps are build artifacts even if emitted outside js-out
*.js.map

# --- Legacy / old output paths (from pixel-slab era) ---
/public/js/
/public/js/**
/public/js-out/
/public/js-out/**
/public/slab.js
/public/consol.js

# --- Runtime / local state (never commit) ---
/data/
/db/
/datalevin/
/var/
/tmp/
/logs/
/log/
/*.log

# If you keep a local state file at repo root
/state.edn
/state.clj
