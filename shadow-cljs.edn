{:source-paths ["src-cljs"]

 :builds
 {:consol
  {:target :browser

   :output-dir "resources/public/js-out"
   :asset-path "/js-out"

   :modules
   {:consol {:entries [consol.main]
             :output-to "resources/public/js-out/consol.js"}}

   ;; Dev-only settings
   :dev {:compiler-options {:optimizations :none}}

   ;; Release defaults (shadow will optimize appropriately)
   :release {}}}}
